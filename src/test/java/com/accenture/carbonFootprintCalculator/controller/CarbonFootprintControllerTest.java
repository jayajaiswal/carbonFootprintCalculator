package com.accenture.carbonFootprintCalculator.controller;

import com.accenture.carbonFootprintCalculator.model.InputFactors;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
//import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.when;

/*
@ExtendWith(MockitoExtension.class)
@RunWith(JUnitPlatform.class)
class CarbonFootprintControllerTest {

    @Mock
    CarbonFootprintController footprintController;

    @Test
    void calculateFootprint() {

        //when(footprintController.calculateFootprint(any(InputFactors.class))).thenReturn(true);


    }
}*/
